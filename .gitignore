# Environment files (may contain credentials)
.env
.env.local
.env.*
!.env.example
!.env.docker.example

# Ralph config (should be in ~/.ralph.env, but just in case)
ralph.env
*.env

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/

# Logs
*.log

# Docker
.env.docker

# Security - Private keys and certificates
*.key
*.pem

# Security - Credentials files
credentials
credentials.*
*secret*
*password*
*token*

# Security - SSH and crypto
.ssh/
*.pub
*.p12
*.pfx

# Security - Cloud provider credentials  
.aws/
.azure/
.gcp/
gcloud/

# Security - Backup files that might contain secrets
*.backup
*.bak
*.old
*.orig
