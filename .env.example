# Ralph Configuration
# Copy this file to .env and fill in your values
# Then source it before running ralph: source ~/.ralph.env

# ============================================
# SLACK NOTIFICATIONS (optional)
# ============================================
# Get your webhook URL from: https://api.slack.com/messaging/webhooks

# Required for Slack notifications (leave empty to disable)
RALPH_SLACK_WEBHOOK_URL=""

# Optional: Override the default Slack channel
# RALPH_SLACK_CHANNEL="#dev-notifications"

# Optional: Customize the bot username (default: "Ralph")
# RALPH_SLACK_USERNAME="Ralph Bot"

# Optional: Customize the bot icon emoji (default: ":robot_face:")
# RALPH_SLACK_ICON_EMOJI=":robot_face:"

# ============================================
# USAGE
# ============================================
# Option 1: Export in your shell profile (~/.bashrc, ~/.zshrc)
#   export RALPH_SLACK_WEBHOOK_URL="https://hooks.slack.com/services/XXX/YYY/ZZZ"
#
# Option 2: Create a ~/.ralph.env file and source it
#   echo 'export RALPH_SLACK_WEBHOOK_URL="https://hooks.slack.com/services/XXX/YYY/ZZZ"' > ~/.ralph.env
#   source ~/.ralph.env
#
# Option 3: Pass inline when running ralph
#   RALPH_SLACK_WEBHOOK_URL="https://hooks.slack.com/..." ~/ralph/ralph.sh ./plan.md
